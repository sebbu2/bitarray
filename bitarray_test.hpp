#ifndef BITARRAY_TEST_HPP
#define BITARRAY_TEST_HPP

#include "bitarray.hpp"

namespace std {
	template<class _Tp, class _Alloc> class vector;
};

void run(unsigned int *success, unsigned int *fail, unsigned int *total, fptr fpt);
int main(void);

#endif
//
